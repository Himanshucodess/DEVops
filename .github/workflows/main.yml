name: First Workflow

on:
  push:

jobs:
  hello:

    runs-on: ubuntu-latest

    steps:

      - name: Print Hello
        run: echo "Hello DevOps"

      - name: Show Files
        run: ls -la

      - name: Print Date
        run: date
